// Shared content prop types for homepage section components

export interface StatItem {
  number: string;
  label: string;
  labelTH: string;
}

export interface ServiceItem {
  icon: string;
  title: string;
  titleTH: string;
  desc: string;
}

export interface TestimonialItem {
  name: string;
  role: string;
  company: string;
  text: string;
  stars: number;
}
